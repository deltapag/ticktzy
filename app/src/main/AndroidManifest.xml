<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ticketzy.NoActionBar"
        tools:targetApi="31">

        <activity android:name=".presentation.base.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".presentation.home.HomeActivity"
            android:exported="true"/>

        <activity android:name=".presentation.sale.ui.SaleActivity"
            android:exported="true"/>

        <activity android:name=".presentation.config.ConfigActivity"
            android:exported="true"/>

        <activity android:name=".presentation.charge.ChargeActivity"
            android:exported="true"/>

        <activity android:name=".presentation.config.ConfigSitefInfosActivity"
            android:exported="true"/>

        <activity android:name=".presentation.cashier.start.ActivityCashierStart"
            android:exported="true"/>

        <activity android:name=".presentation.cashier.finish.ActivityCashierFinish"
            android:exported="true"/>

        <activity android:name=".presentation.cashier.home.ActivityCashierHome"
            android:exported="true"/>

        <activity android:name=".presentation.cashier.sangria.CashierSangriaActivity"
            android:exported="true"/>

        <activity android:name=".presentation.cashier.reinforce.CashierReinforceActivity"
            android:exported="true"/>

        <activity android:name=".presentation.sitef.ActivitySitefHome"
            android:exported="true"/>

    </application>

</manifest>